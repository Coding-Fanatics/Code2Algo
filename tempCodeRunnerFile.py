elif(len(range_)==2):
            #     fin = "repeat step {} to {} for {}={} until {}={} step +1".format(start_step+1,next_step,variable,variable,range_[0],range[1])
            # elif(len(range_)==3):
            #     fin = "repeat step {} to {} for {}={} until {}=10 step +1".format(start_step+1,next_step,variable,variable,range_[0],range[1])
            # else:
            #     raise "ValueError"